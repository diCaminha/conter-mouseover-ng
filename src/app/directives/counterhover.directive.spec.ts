import { CounterhoverDirective } from './counterhover.directive';

describe('CounterhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CounterhoverDirective();
    expect(directive).toBeTruthy();
  });
});
